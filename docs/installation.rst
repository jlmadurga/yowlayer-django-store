============
Installation
============

At the command line::

    $ easy_install yowlayer-django-store

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv yowlayer-django-store
    $ pip install yowlayer-django-store
    

